#pragma once

#include "pch.hpp"
#include "runtime.hpp"

//void evaluate_if_statement(ListTokenPtr::iterator& it, ListTokenPtr::iterator& end, if_block* block);
//void evaluate_else_statement(ListTokenPtr::iterator& it, ListTokenPtr::iterator& end, else_block* block);
//
//bool handle_if_conditional(ListTokenPtr::iterator& it, ListTokenPtr::iterator& end, std::unique_ptr<stack_t::block_t>& c);